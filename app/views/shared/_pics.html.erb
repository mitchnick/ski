<ul>
	<% @photos.each do |photo| %>
		<div>
			<%= link_to image_tag(photo.image_url(:thumb)), [photo.mountain, photo] %>
			<%= link_to photo.name, [photo.mountain, photo] %> -- <%= photo.gnars.count %>
		</div>
	<% end %>
	<%= will_paginate @photos %>
</ul>