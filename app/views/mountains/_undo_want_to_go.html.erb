<%= form_for(@mountain.my_mountains.where("user_id = ? AND type = ?", current_user.id, 0).first, html: { method: :delete}) do |f| %>
	<%= f.submit "Unclick - I want to go here", class: "btn btn-large btn-primary" %>
<% end %>