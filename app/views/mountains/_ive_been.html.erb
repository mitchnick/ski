<%= form_for @mountain.my_mountains.build do |f| %>
	<%= f.hidden_field :mountain_id %>
	<%= f.hidden_field :type, :value => 1 %>
	<%= f.submit "I've been here", class: "btn btn-large btn-primary" %>
<% end %>