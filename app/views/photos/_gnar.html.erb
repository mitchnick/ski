<%= form_for(@photo.gnars.build, :remote => true) do |f| %>
	<%= f.hidden_field :photo_id %>
	<%= f.submit "Give Gnar", class: "btn btn-large btn-primary btn-block gnar_describe", :title => "Give props to this photo and vote it up"%>
<% end %>