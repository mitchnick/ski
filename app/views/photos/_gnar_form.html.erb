<div class="gnar_form">	
	<% if @gnars.find_by_user_id(current_user.id) %>
		<%= render 'ungnar' %> 
	<% else %>
		<%= render 'gnar' %> 
	<% end %>
</div>
<div id="hint"><a href="#" data-toggle="tooltip" title="Give props to this photo and vote it up">What is Gnar?</a></div>